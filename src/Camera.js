import React, {Component} from 'react';
import {View, Text, Image} from 'react-native';
import App from './Components/AltTutor/app';

export default class Camera extends Component {
    render() {
        return (
            <App/>
        );
    }
}