import React, {Component} from 'react';
import {View, Text, Image} from 'react-native';
import Nav from './Components/AltTutor/nav';

export default class Camera extends Component {
    render() {
        return (
            <Nav/>
        );
    }
}